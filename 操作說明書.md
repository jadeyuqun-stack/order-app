# 玉群環境科技 點餐系統 — 操作說明書

## 一、系統架構

本系統為員工每日點餐平台，分為兩個角色入口：

| 角色 | 位置 | 用途 |
|------|------|------|
| 一般員工 | 首頁 / 點餐 | 查看今日餐廳、自主填寫菜色與價格 |
| 管理員 | 管理後台 | 設定餐廳、開單、維護訂單、查看月報 |

---

## 二、首頁（員工端）

### 2.1 首頁
**路徑：** `/`

- 顯示今日餐廳名稱與截止時間
- 若未開單則顯示「今日尚未開放點餐」
- 下方顯示今日訂單匯總（按菜色歸類）與訂購狀況明细
- 匯總表含餐廳名稱欄位

### 2.2 點餐頁面
**路徑：** `/menu`

1. 輸入姓名（自動儲存至瀏覽器，下次不用重填）
2. 逐筆加入餐點：菜色名稱 + 價格 + 數量
3. 加入清單後可刪除項目
4. 按下「一次送出全部」完成點餐
5. 送出成功後可「繼續點餐」或「回首頁」

> **注意：** 截止時間一到，系統自動關閉點餐，員工無法再送出。

---

## 三、管理後台

**路徑：** `/dashboard/admin`
**登入：** 帳密預設 `admin` / `admin123`

後台分為四個分頁：

---

### 3.1 餐廳

**功能：** 新增、刪除、匯出/匯入餐廳資料與菜單照片

#### 新增餐廳
1. 輸入餐廳名稱
2. （選填）上傳菜單照片（支援 JPG/PNG）
3. 按下「新增」

#### 刪除餐廳
- 卡片右下角「刪除」按鈕
- 點擊後需確認，刪除前會自動清理關聯的開單與訂單資料

#### 匯出 / 匯入
- **匯出餐廳資料**：將所有餐廳名稱與菜單照片（Base64）匯出為 JSON 檔案
- **匯入餐廳資料**：選擇先前匯出的 JSON 檔案，重複餐廳名稱會跳過

> **用途：** 方便在不同環境（本地 / Render）之間轉移餐廳設定。

---

### 3.2 開單

**功能：** 設定每日可點餐的餐廳與截止時間

#### 建立開單
1. 選擇餐廳（下拉選單）
2. 選擇日期
3. 選擇截止時間（日期 + 時間，採台灣本地時間）
4. 按下「建立」

#### 管理開單
每個開單卡片顯示：
- 餐廳名稱、日期、截止時間（台灣時間）
- 狀態標籤：「點餐中」或「已截止」

可執行操作：
| 操作 | 說明 |
|------|------|
| **修改截止** | 點擊後出現 inline 編輯框，可調整截止時間 |
| **訂購截止** | 提前關閉點餐（不需等到設定時間） |

#### 自動截止
- 系統每次讀取 `/api/daily-orders` 時會自動檢查截止時間
- 超過截止時間的開單自動變更為「已截止」狀態

---

### 3.3 訂單

**功能：** 查看與維護每日訂單

#### 查詢
- 選擇日期查詢該日訂單
- 可切換排序方式：依姓名 / 依菜色

#### 編輯數量與單價
- 點擊「數量」欄位（顯示 `數量 (NT$單價)`）
- 展開 inline 編輯框，可同時修改數量與單價
- 按下「存」確認，「取」取消

#### 刪除訂單
- 點擊「刪除」按鈕，需二次確認

#### 底部合計列
- 自動計算總數量與總金額

---

### 3.4 月報

**功能：** 查看月度訂單報表、匯出 CSV、清除歷史資料

#### 產生報表
1. 選擇年份與月份
2. 按下「產生報表」
3. 顯示兩個表格：
   - **總彙總**：每人當月累積的訂餐次數與總金額
   - **訂單明細**：每筆訂單的日期、時間、餐廳、菜色等詳細資訊

#### 匯出 CSV
- 按下「匯出 CSV」
- 下載單一 CSV 檔案，內容左右並排：
  - 左側（A-D 欄）：總彙總（部門、姓名、訂餐次數、總金額）
  - 右側（F-K 欄）：訂單明細（日期、時間、餐廳、姓名、菜色、數量、單價、金額）
  - 中間 E 欄為空白分隔
- 第一行為動態月份標題（如「7月總彙總」）

#### 清除本月明細
- 位於兩個表格下方的右側
- 紅色按鈕「清除 YYYY年MM月 明細」
- 點擊後需二次確認
- 清除後自動重新產生報表
- **用途：** 避免資料庫檔案持續膨脹，建議定期清理歷史月份

---

## 四、常見問題

### Q1：截止時間顯示不對？
系統統一使用台灣時間（UTC+8）顯示。若發現差異，可能是舊資料尚未轉換，請重新建立開單即可。

### Q2：截止時間到了還能點餐？
- 系統會在每次讀取時自動檢查
- 若仍無法截止，請管理員手動進入「開單」分頁按下「訂購截止」

### Q3：刪除餐廳失敗？
- 系統已啟用外鍵約束，刪除前會先清理關聯的開單與訂單
- 若仍有錯誤，請先手動清除該餐廳的開單與訂單再刪除

### Q4：如何備份資料？
- 餐廳資料可透過「匯出餐廳資料」匯出 JSON
- 訂單資料可透過「匯出 CSV」匯出完整報表

---

## 五、預設帳密

| 項目 | 值 |
|------|------|
| 管理員帳號 | `admin` |
| 管理員密碼 | `admin123` |

> 建議首次使用後修改密碼。
